/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: { serverActions: { bodySizeLimit: '2mb' } },
  // important si tu importes des packages internes du monorepo
  transpilePackages: ['@fitnest/db'],
};

export default nextConfig;
