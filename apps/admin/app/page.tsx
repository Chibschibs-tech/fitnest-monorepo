export const metadata = { title: "Fitnest — Admin" };

export default function AdminHome() {
  return (
    <main style={{padding: 24}}>
      <h1>Admin — Dashboard</h1>
      <p>Si vous voyez cette page sur le port 3001, l’admin tourne bien ✅</p>
    </main>
  );
}
